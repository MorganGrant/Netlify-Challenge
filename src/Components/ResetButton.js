import React from 'react'

const ResetButton = props =>
    
    <button
        className="reset--button"
        onClick={props.click}>
        Reset the Game!
    </button>
    

    export default ResetButton
